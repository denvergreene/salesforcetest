<?xml version="1.0" encoding="utf-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Domain__c</fullName>
    <externalId>false</externalId>
    <formula>IF(
    FIND(
      "/",
SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(Website,"www.",""),"http://",""),"www2.",""),"www3.",""),"www1.",""),"https://",""),
      1
    )&gt;1

,
LEFT(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(Website,"www.",""),"http://",""),"www2.",""),"www3.",""),"www1.",""),"https://",""),FIND("/",SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(Website,"www.",""),"http://",""),"www2.",""),"www3.",""),"www1.",""),"https://",""))-1)
,
SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(Website,"www.",""),"http://",""),"www2.",""),"www3.",""),"www1.",""),"https://","")
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Domain</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
